schema = 1

# Temporary lint debt belongs here instead of weakening workspace policy.
# Each entry must include lint, path, owner, reason, and expires. Expired debt
# fails `cargo xtask check-lint-policy`.
#
# [[debt]]
# lint = "clippy::indexing_slicing"
# path = "crates/example/src/parser/table.rs"
# owner = "parser"
# reason = "Generated table access; replace with checked table abstraction."
# expires = "2026-07-01"
