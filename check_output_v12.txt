error: failed to select a version for `windows`.
    ... required by package `racing-wheel-engine v0.1.0 (H:\Code\Rust\OpenRacing\crates\engine)`
versions that meet the requirements `^0.62.2` (locked to 0.62.2) are: 0.62.2

package `racing-wheel-engine` depends on `windows` with feature `Win32_UI_Pnp` but `windows` does not have that feature.
 package `windows` does have feature `Win32_UI_Input`


failed to select a version for `windows` which could resolve this conflict
