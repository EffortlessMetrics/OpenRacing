# Rustfmt configuration for consistent code formatting
# CI uses stable rustfmt — only stable-compatible options here.

# Edition to use for formatting
edition = "2024"

# Maximum width of each line
max_width = 100

# Number of spaces per tab
tab_spaces = 4

# Use spaces instead of tabs
hard_tabs = false

# Use small heuristics to improve formatting
use_small_heuristics = "Default"

# Reorder imports
reorder_imports = true

# Reorder modules
reorder_modules = true

# Remove nested parens
remove_nested_parens = true

# Merge derives
merge_derives = true

# Use field init shorthand
use_field_init_shorthand = false

# Force explicit types
force_explicit_abi = true